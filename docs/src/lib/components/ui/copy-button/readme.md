# Copy Button Component

A button component that copies text to clipboard with optional feedback via tooltip.

## Files

- `copy-button.svelte`: Main button component that handles copy-to-clipboard functionality
- `copy-button-tooltip.svelte`: Optional tooltip component to provide feedback when copy succeeds
- `index.ts`: Barrel export for easy importing all copy-button components
