# Button Group Component

Groups multiple buttons together as a single unit.

## Files

- `button-group.svelte`: Container component for grouping related buttons with unified styling
- `index.ts`: Barrel export for easy importing from the component folder
